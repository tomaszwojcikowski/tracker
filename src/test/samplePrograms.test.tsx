// Sample program tests removed.
// The app no longer ships built-in sample programs by default.
import { describe, it } from 'vitest';

// Sample program tests removed (sample programs are not shipped in this build).
// Keep a skipped suite so Vitest doesn't fail with "No test suite found".
describe.skip('Sample Programs (removed)', () => {
	it('placeholder', () => {
		// intentionally empty
	});
});
